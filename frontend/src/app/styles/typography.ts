import { colors } from "./colors";

export const typography = {
    title: {
        fontSize: 28,
        fontWeight: "800",
        textAlign: "center",
        color: colors.text
    },
    subtitle: {
        fontSize: 20,
        fontWeight: "500",
        color: colors.text
    },
    body: {
        fontSize: 16,
        fontWeight: "400",
        color: colors.text
    },
    metric: {
        fontSize: 26,
        fontWeight: '600',
        color: colors.primary
    },
    audio: {
        fontSize: 24,
        fontWeight: "600",
        textAlign: "center",
        color: colors.text
    }
};
